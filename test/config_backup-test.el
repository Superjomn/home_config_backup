;;; config_backup-test.el --- Tests for config_backup
(ert-deftest my-test()
  (should (equal 1 1))
  (should-not (equal 1 2))
  )

(ert-deftest my-test1()
  (should (equal 1 1))
  (should-not (equal 1 2))
  )

;;; config_backup-test.el ends here
